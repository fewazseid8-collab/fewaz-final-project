# Backend environment variables (do NOT commit real secrets)
# Set these in Render Dashboard (Service > Environment) or in your local `backend/.env` for development.

# MongoDB connection string (production)
MONGODB_URI=mongodb+srv://<USER>:<PASSWORD>@cluster0.mongodb.net/<DBNAME>?retryWrites=true&w=majority

# JWT secret (use a long random string)
JWT_SECRET=replace_with_a_long_random_secret_here

# Node environment
NODE_ENV=production

# Optional: port (Render provides its own port, but you can override)
PORT=4321

# Optional service keys
# SENTRY_DSN=
# SOME_EMAIL_API_KEY=
